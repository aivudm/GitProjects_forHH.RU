program DBNTConnect;

uses
  Forms,
  SetConnect in 'SetConnect.pas' {Form1},
  LoadPicture in 'LoadPicture.pas' {Form2};

{$R *.RES}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
